// Create a function that takes a boolean variable flag and returns it as a string.

function boolToString(flag) {
    if (flag === true) {
        return "true";
    } else {
        return "false";
    }
}
console.log(returnString(true));
console.log(returnString(false));